[green_agent]
agentbeats_id = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
env = { API_KEY = "${GOOGLE_API_KEY}", LOG_LEVEL = "INFO" }

[[participants]]
name = "Purple Agent"
agentbeats_id = "yyyyyyyy-yyyy-yyyy-yyyy-yyyyyyyyyyyy"
env = {}

[config]
# 如果你的 Green Agent 需要特定的评测参数（例如指定漏洞任务），在这里添加
# 例如: task_id = "arvo:1065"
